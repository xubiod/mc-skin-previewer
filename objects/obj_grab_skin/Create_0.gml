/// @description default username

// Inherit the parent event
event_inherited();

name = "notch";
progression = -1;

name2uuid = noone;
uuid2texture = noone;
download = noone;

// even if i check it exists (when it doesn't) it still complains
json_data = ds_map_create();